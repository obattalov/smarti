<nav>
                    <a class="btn btn-default" href="{{ route('galery') }}">
                        {{ __('Galery') }}
                    </a>
                    <a class="btn" href="{{ route('editor') }}">
                        {{ __('Editor') }}
                    </a>
                    <a class="btn btn-default" href="{{ route('galery.completedb') }}">
                        {{ __('Autocomplete DB') }}
                    </a>


</a>
